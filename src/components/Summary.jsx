import { formatINR } from '../utils/cart'
export function Summary({ cart, action, disabled = false }) { const subtotal = cart.reduce((sum, item) => sum + item.originalUnitPrice * item.quantity, 0); const total = cart.reduce((sum, item) => sum + item.finalUnitPrice * item.quantity, 0); return <div className="summary"><div><span>Original subtotal</span><strong>{formatINR(subtotal)}</strong></div><div className="discount-row"><span>Collection discount</span><strong>- {formatINR(subtotal - total)}</strong></div><div className="summary-total"><span>Total <small>INR</small></span><strong>{formatINR(total)}</strong></div><button className="button button-dark full" onClick={action} disabled={disabled}>{disabled ? 'Nothing to checkout' : 'Continue'} <span aria-hidden="true">↗</span></button></div> }
